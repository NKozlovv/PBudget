/**
 * Build identity, surfaced in the sidebar so the user can confirm a
 * deploy is live (the legacy app had a marker at the bottom-right
 * corner — same idea here).
 *
 * Bump this on every meaningful change so the sidebar visibly
 * reflects what's deployed.
 */

export const BUILD_VERSION = 'v3.0.0-α';
export const BUILD_DATE = '2026-09-17';
export const BUILD_NOTE = 'Liquid-glass rehaul in progress — foundations + top nav';
